<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:gravity="center"
    tools:context="com.example.android.tictactoe.MainActivity">

    <!-- Everything written here will be child of Relative Layout-->

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="PLAY GAME"
        android:background="@color/colorPrimary"
        android:textSize="40dp"
        android:textColor="@color/white"
        android:gravity="center"
        android:layout_margin="0dp"
        android:padding="30dp"
        android:id="@+id/bPlay"
        />
    <!-- Width and height are a must      dp=densitypixel(UNIT)           standard:640dp height and 360dp width of entire screen-->

    <Button
        android:layout_below="@+id/bPlay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="INSTRUCTIONS"
        android:background="@color/colorPrimary"
        android:textSize="40dp"
        android:textColor="@color/white"
        android:gravity="center"
        android:layout_marginTop="30dp"
        android:padding="30dp"
        />

</RelativeLayout>
